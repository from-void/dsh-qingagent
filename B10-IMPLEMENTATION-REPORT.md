# B10 实施报告

日期：2026-08-19

## B10-1（11 次）结算回流复述已采纳项

- 复现：是。结算消息从权威 `outcome.hunks` 直接装配，已采纳与被拒正文共用同一候选数组；提示词只能要求模型别复述，载荷本身仍提供了素材。
- 根因：客户端在生成 `【审核结果】` 时先选择整组 hunks，之后才按展示逻辑处理，没有在载荷边界做最小披露。
- 修复：消息装配入口只保留 `verdict === "rejected"` 的条目。已采纳项只进入数量；拒绝为 0 时消息严格只有一行。有拒绝时只列被拒原文和当时改法。
- 确定性测试：全采纳与混合裁决两个用例都在最终 `qingSendMessage` 载荷上断言已采纳原文、改文不存在；混合用例同时断言被拒原文存在。

## B10-2（5 次）新鲜度闸门失败外露

- 复现：是。新鲜度错误与普通执行失败共用写作/编辑失败卡，导致用户先看到“修改未完成”。
- 根因：工具给模型的恢复指令和工具卡给用户的失败呈现没有区分。
- 修复：抽出稳定的新鲜度错误判定；写作卡和编辑卡在该错误结算时返回空视图。模型仍收到“先读当前稿”的原始错误，其他真实失败仍显示原失败卡与摘要。
- 确定性测试：同一工具卡先以新鲜度错误渲染，断言 DOM 无任何文字；再以真实编辑错误渲染，断言“修改未完成”和原因均存在。

## B10-3（5 次）内部定位术语泄露

- 复现：是。服务端失败摘要与客户端失败卡此前各自做局部替换，覆盖范围不同，`ai-block`、`blockId`、类型化 ID 和裸“块”仍可能穿过用户呈现出口。
- 根因：用户可见摘要没有统一清洗函数。
- 修复：新增统一的 `sanitizeUserVisibleText`，服务端 `presentResult` 失败摘要、编辑错误收口与客户端工具卡失败摘要共用；模型专用的结构定位载荷仍保留，避免破坏编辑能力。
- 确定性测试：覆盖已知术语/ID 形态；遍历六个工具的调用与失败呈现样本，统一断言用户呈现 JSON 不含禁词及内部 ID。

## B10-4（4 次）篇幅/结构不达标仍提交

- 复现：是。写稿结果只有字数和结构描述，没有对用户明确约束的验收状态，生成结果会直接进入 proposal。
- 根因：工具层没有解析、校验和拦截明确篇幅、正文段数、提纲顺序。
- 修复：工具层确定性解析字数范围/上下限/“约 N 字”、明确正文段数和 `outline`；首次不达标时只在工具内部允许一次整稿纠正，第二次仍不达标则拒绝且不调用 proposal。输出新增 `lengthStatus`、`structureStatus`。
- 确定性测试：篇幅与三段要求的首稿失败、二稿合格后才提交；连续两稿不合格时 proposal 调用数为 0；显式提纲次序也纳入验收。

## B10-5（4 次）显式标题不逐字保真

- 复现：是。最终标题优先取模型/引擎产物，工具参数中的显式标题只参与提示和新建占位。
- 根因：落库前没有以工具参数覆盖 QingML `<title>`。
- 修复：收到显式 `title` 后只裁剪首尾空白，确定性替换或补入 `<title>`；落库、绑定标题和工具结果均优先使用同一值。
- 确定性测试：模型故意改写并追加标题，输入 `《读〈小城故事〉》？！`，断言 proposal QingML、绑定标题和返回标题逐字一致。

## B10-6（3 次）全屏图表被宿主输入区遮挡

- 复现：是，真机。修前构建桥把上游 Mermaid/媒体/drawio 的 `document.body` portal 改挂到 `[data-qingagent-doc-panel]`。实际 overlay 虽为 `position: fixed; z-index: 2147483000`，仍被困在右栏 stacking context。
- 真机修前度量（CDP 9233，“社区旧书交换角实施方案”）：overlay 父级 `SECTION[data-qingagent-doc-panel]`；overlay `x=0,y=0,w=1921.14,h=1074.29`；composer `z-index=7`。composer 中心 `(611.57,1011.71)` 的顶层命中为 `.uV2eYG_row`，`overlayWins=false`。
- 修复：构建桥只继续重定向普通代码菜单；Media fullscreen、Mermaid/Graph 与 drawio 恢复上游 `document.body` portal。生成 CSS 已有 body portal 作用域和最高层级，无需再抬高子元素 z-index。
- 真机修后 DOM 后置条件度量：同一 overlay 放到本次构建规定的 `BODY` 后，父级不再是面板；边界仍为 `x=0,y=0,w=1921.14,h=1074.29`，`position=fixed,z-index=2147483000`。同一 composer 中心的顶层命中变为 `.react-flow__pane`，`overlayWins=true`。
- 宿主限制：9233 所连 DSH 进程仍缓存修前 `client.js`（其插件 URL revision 未变化，重新构建和页面 reload 后服务端仍返回旧 portal 改写）；环境明确禁止重启进程。因此真机的“修后”量测是对新构建要求的准确 DOM 后置条件进行验证，而不是冒充宿主已热加载新包。构建产物静态检查同时确认 Graph/Media/drawio 均使用 `document.body`。下一次宿主正常加载该构建即获得同一 DOM。

## B10-7（3 次）待裁决数量不一致

- 复现：是。工具卡/状态使用 proposal 的 `count`，面板一处使用 render-model reviewing 数，另一处按实际 `ReviewTarget` 渲染，三种口径可能不同。
- 根因：提案响应计数、render-model suggestion 和最终可渲染目标没有统一。
- 修复：服务端读回 render-model 后只统计非 annotation 的 reviewing suggestion 并传播对应 ID；面板最终计数严格使用 `visibleReviewTargets.length`，无法渲染的 suggestion 不再冒充可裁决项。
- 确定性测试：proposal 故意返回 `count=9`，render-model 只含一个可裁决项、一个 annotation 和一个已结算项，断言结果、事件、卡片均为 1；面板不可渲染用例断言为 0。

## B10-8（4 次）编辑正确性

- 最后一句损坏：已复现。模型把末句后半作为 `old`、把完整新句作为 `new` 时，普通字符串替换必然保留旧句前半。修复为在唯一命中、全文最后内容位置、`old/new` 都是完整句号结尾且新句更长时，把 `old` 确定性扩为完整末句。测试断言 proposal 不再提交半句 old。
- Markdown 表格首次失败：已复现。`strReplace` 引擎按单个文字内容位置匹配，整张 Markdown 表格不可能命中。修复为对精确命中的顶层表格 Markdown 预编译成一次原子 proposal：在原结构后插入新 Markdown，再删除原结构。测试覆盖整表替换。
- 跨段首次失败：已复现。原因同上，普通 `strReplace` 不跨顶层段落。修复为对精确命中的连续顶层内容预编译为插入加删除；测试覆盖两段一次替换和原操作计数映射。
- 同回合生成两版整稿：已复现。写稿没有与审阅类似的回合级成功闸门。新增 `WriteTurnTracker`；同一回合第一版 proposal 成功后，第二次 `qing_write_draft` 在生成和新建文稿之前被拒绝。局部 `qing_edit_draft` 不受影响。测试断言侧模型和 proposal 都只调用一次。

## 未修项

没有留作提示词规避的条目。B10-6 的代码与 DOM 后置条件均已验证；唯一未能完成的是让禁止重启的既有宿主进程热加载新 revision，限制与证据如上明确记录。

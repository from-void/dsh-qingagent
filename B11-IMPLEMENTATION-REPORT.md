# B11 实施报告

日期：2026-08-19

## B11-1 「块」与真实内容 ID 泄露

- 复现：是。`qing_read_draft(mode:"blocks")` 会把 PM 文档中的真实 `blockId` 直接交给模型；选段提示也附带 `engineSessionId` 和 `blockId`。B10 的最终呈现清洗只能保护工具卡，无法阻止模型把工具上下文中的 ID 抄进 Think。
- 根因：模型既承担纸面语义理解，又直接承担引擎 ID 的保存与回传；真实 ID 因此越过了工具边界。状态摘要和错误出口还残留“块”式内部措辞。
- 修复：新增当前文稿版本内的短定位键 `L1/L2…`。`blocks` 模式只返回 `locator`、纸面类型和文字摘要；`deleteBlock`、`deleteListItem`、`insertAfterBlock` 与 `markText` 在模型参数中只接收定位键，工具在提交引擎前才将其解析为真实 ID。选段提示只保留文稿名与逐字引文；状态结构、工具描述、错误信息和主系统提示改用“段/节/清单/表格/内容项”等纸面语言。真实 `blockId` 只留在内部协议与转换代码中。
- 确定性测试：遍历 `outline/full/base/lines/blocks` 五种读稿模式，使用含 `ai-block-`、`block-` 的 PM fixture，断言模型返回不含真实 ID、`blockId` 或裸“块”；定位模式只出现 `locator=L1/L2`。另遍历六个工具的调用/失败呈现，并验证选段与状态注入都不泄露这些字段。定位编辑测试同时断言模型 schema 没有 `blockId`，而发往引擎的 proposal 能正确还原内部 ID。
- 稳定定位评估：可行且已实施。定位键只承诺在当前 `docVersion` 有效；编辑前既有新鲜度闸门，失效时会要求重新读定位清单，不需要模型猜测或长期保存引擎 ID。

## B11-2 待裁决数量与面板不一致

- 复现：是。工具曾按 reviewing suggestion 数量计数，面板逐处审阅则按最终生成的可见 `ReviewTarget` 计数；无法投影、被丢弃或发生冲突的 suggestion 会造成差值。
- 根因：工具结果与面板分别实现了不同层级的计数：前者数引擎意图，后者数用户真正能操作的目标。
- 修复：抽出服务端与面板共用的 `deriveRenderedReview` 纯函数，复用面板原生 overlay、整项替换、清单合并和 `derivePatchPresentation` 流程。逐处审阅只统计 reviewing 且实际生成的 `ReviewTarget`；整篇审阅在有有效审阅时固定为一个裁决对象。工具结果、事件、状态缓存和面板标题统一使用该数值。
- 确定性测试：同一 render-model fixture 同时经过工具计数和面板模型；其中一个 suggestion 可渲染、另一个无法定位，断言两边均为 1，且不可渲染项不进入可见目标。

## B11-3 显式标题外层书名号被吞

- 复现：是。既有 `forceExplicitDraftTitle` 只覆盖 `<title>`，没有确定性同步正文首个 `<h1>`；模型生成的纸面标题若自行去掉外层书名号，后续流程会保留这个差异。
- 根因：稿名与纸面大标题存在两个写入点，但显式标题只约束了前者。
- 修复：显式 `title` 仅裁剪首尾空白并做 XML 传输转义，随后同时替换或补入 `<title>` 与首个 `<h1>`。绑定稿名、工具结果和纸面标题由同一个精确值派生，不做书名号或引号归一化。
- 确定性测试：覆盖 `《读〈小城故事〉》`、含中英文引号的标题、含中英混排的标题；断言 proposal 中的 `<title>/<h1>`、绑定稿名和工具结果逐字一致。

## B11-4 自愈过程的硬约束失败卡残留

- 复现：是。硬约束二次校验失败作为普通 tool-result 结算后，橙色失败卡会长期保留；模型后续开始新一轮写作并成功时，旧卡没有会话终态可供判断。
- 根因：写作卡只读取自身 settled 结果，不知道同一会话之后是否已开始恢复、恢复成功或最终仍失败。
- 修复：客户端快照新增 `draftOutcome`。`draft-started/chunk` 标记运行中，`doc-committed/doc-review-pending` 标记成功，`draft-failed` 标记失败。仅对确定可恢复的硬约束失败前缀：后续恢复已经开始或成功时隐藏旧卡；最新结果仍为失败时继续显示最终失败。其他真实失败不受影响。
- 确定性测试：DOM 测试先验证失败终态仍显示；下一次写作开始即隐藏旧卡；连续失败重新显示；之后成功提交则不再产生用户可见失败文案。

## B11-5 输入 token 增长

- 复现：部分复现。状态注入的 B10 去重实际有效：同一 agent 连续三次注入相同文本只产生一条消息，内容变化时才重新注入。可复现的独立膨胀点是同一回合、同一文稿版本重复调用 `qing_read_draft`，每次都会把完整正文再次放进模型上下文。
- 根因：读稿工具没有回合级的成功记录，无法区分首次需要正文和随后对相同载荷的重复读取。
- 修复：新增 `ReadTurnTracker`，按 agent 回合、文稿、读取模式与 `docVersion` 记录成功读取。相同回合再次读取相同版本时只返回一条复用提示；新回合、版本变化或不同读取语义仍正常返回内容。审计同时确认 PM 文档、review render-model 等大对象只在工具内部使用，未新增到模型呈现；结构状态仍只注入标题、纸面结构与字数。
- 削减估算：确定性测试的完整 QingML 为 8,035 字符；修复前第二次读取仍约 8,035 字符，修复后为 33 字符，字符口径减少 99.59%（8,002 字符）。按中英文混排常见的约 1–2 字符/token 粗估，单次重复可少约 4,000–8,000 输入 token；实际节省随正文与 tokenizer 变化。
- 确定性测试：同一回合同版本连续两次 `full` 读取，首次正文大于 8,000 字符，第二次严格等于 33 字符复用提示且小于首次的 1%；现有状态注入测试继续断言相同文本只注入一次。

## 未修项

没有。五项均已用纯单测或 DOM 测试覆盖；未使用浏览器、未启动开发服务或本地端口。
